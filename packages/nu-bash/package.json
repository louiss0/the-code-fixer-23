{
  "scripts": {
    "build": "vite build --config build.config.ts --mode production",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit",
    "test": "vitest run --config build.config.ts --passWithNoTests",
    "lint": "eslint .",
    "format": "biome format --write .",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run build"
  },
  "version": "0.2.2",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  }
}

{
  "name": "@code-fixer-23/enums",
  "version": "0.1.0",
  "type": "module",
  "description": "Enum helpers for string, number, and labeled enum values.",
  "keywords": [
    "enum",
    "typescript",
    "utilities"
  ],
  "scripts": {
    "build": "pnpm exec tsup --config tsup.config.ts",
    "test": "pnpm exec vitest run --config vitest.config.ts",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.lib.json",
    "lint": "pnpm exec eslint ."
  }
}

{
  "$schema": "https://json-schema.org/schema",
  "$id": "Library",
  "title": "Generate a JSR TypeScript Library",
  "description": "Create a new TypeScript library configured for JSR (JavaScript Registry) publishing",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "directory": {
      "type": "string",
      "description": "Directory where the library will be created (defaults to packages)",
      "default": "packages"
    },
    "importPath": {
      "type": "string",
      "description": "The JSR import path (e.g., @scope/package-name)",
      "x-prompt": "What is the JSR import path? (e.g., @scope/package-name)",
      "pattern": "^@[a-z][a-z0-9-]*/[a-z][a-z0-9-]*$"
    },
    "description": {
      "type": "string",
      "description": "Package description",
      "default": ""
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files",
      "default": false
    },
    "bundler": {
      "type": "string",
      "description": "Bundler to use for building the library",
      "enum": ["none", "esbuild", "tsup"],
      "default": "none",
      "x-prompt": {
        "message": "Which bundler would you like to use?",
        "type": "list",
        "items": [
          {
            "value": "none",
            "label": "None (TypeScript source only - recommended for JSR)"
          },
          {
            "value": "esbuild",
            "label": "esbuild (Fast bundling with minimal configuration)"
          },
          {
            "value": "tsup",
            "label": "tsup (esbuild with better DX and JSR-friendly defaults)"
          }
        ]
      }
    }
  },
  "required": ["name", "importPath"]
}

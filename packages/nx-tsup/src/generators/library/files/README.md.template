# <%= name %>

<%= description || 'A TypeScript library built with Tsup.' %>

## Installation

```bash
pnpm add <%= importPath %>
# or JPD if available
```

## Usage

```ts
import { hello } from '<%= importPath %>';

console.log(hello());
```

## Development

- Build: `pnpm build`
- Watch: `pnpm dev`
- Typecheck: `pnpm typecheck`
<% if (testRunner === 'vitest') { %>- Test: `pnpm test`<% } %>
<% if (testRunner === 'jest') { %>- Test: `pnpm test`<% } %>

<% if (linter === 'eslint') { %>
## Linting
This package uses ESLint (flat config).
<% } %>
<% if (linter === 'biome') { %>
## Linting/Formatting
This package uses Biome.
<% } %>
